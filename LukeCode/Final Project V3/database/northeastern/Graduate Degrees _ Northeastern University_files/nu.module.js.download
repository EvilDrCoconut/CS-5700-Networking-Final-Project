/**
* Created by acheson on 3/28/16.
*/
(function() {
    'use strict';

    // Add any module dependencies here
    angular
        .module('nu', [
            // 'ngAnimate',
            'ngSanitize',
            'infinite-scroll'
        ]);
})();

angular.element(document).ready(function() {
    angular.bootstrap(document.getElementsByTagName('body')[0], ['nu']);
});

/*

When programListing controller loads before compareWidget it does not work.

When compareWidget loads before programListing page it ALWAYS works

 */
